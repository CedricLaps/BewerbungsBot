[project]
name = "bewerbungsbot"
version = "1.0.0"
description = "Lokales Bewerbungsautomatisierungssystem mit Ollama, Playwright und FastAPI"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py312"
